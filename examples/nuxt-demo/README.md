# Nuxt Demo

This is the stock Nuxt starter connected through PickFix as an external target.

No PickFix code, SDK, or bridge setup is installed in this app. PickFix injects
the selection bridge through the preview proxy at runtime.

```bash
pnpm dev:nuxt
```
